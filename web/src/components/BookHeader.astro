---
import BookHeaderInner from './book/BookHeader.astro';

export interface Props {
  title: string;
  author: string;
  year: number;
  rating?: number;
}
const { title, author, year, rating } = Astro.props;
---

<BookHeaderInner title={title} author={author} year={year} rating={rating} />
